apply plugin: 'java'

repositories {
    jcenter()
}

jar {
    manifest {
        attributes(
                'Premain-Class': 'pl.poznan.jagent.Jagent'
        )
    }
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.21'
    testCompile 'junit:junit:4.12'
}

